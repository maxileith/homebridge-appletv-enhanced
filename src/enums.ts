export enum DisplayOrderTypes {
    ARRAY_ELEMENT_START = 0x01,
    ARRAY_ELEMENT_END = 0x00,
}

export enum RemoteControlCommands {
    CHANNEL_DOWN = 'channel_down',
    CHANNEL_UP = 'channel_up',
    DOWN = 'down',
    HOME = 'home',
    HOME_HOLD = 'home_hold',
    LEFT = 'left',
    MENU = 'menu',
    NEXT = 'next',
    PAUSE = 'pause',
    PLAY = 'play',
    PLAY_PAUSE = 'play_pause',
    PREVIOUS = 'previous',
    RIGHT = 'right',
    SELECT = 'select',
    SKIP_BACKWARD = 'skip_backward',
    SKIP_FORWARD = 'skip_forward',
    STOP = 'stop',
    TURN_OFF = 'turn_off',
    TURN_ON = 'turn_on',
    TOP_MENU = 'top_menu',
    UP = 'up',
}